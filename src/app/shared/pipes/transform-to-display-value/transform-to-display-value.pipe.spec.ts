import { TransformToDisplayValuePipe } from './transform-to-display-value.pipe';

describe('TransformToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformToDisplayValuePipe();
    expect(pipe).toBeTruthy();
  });
});

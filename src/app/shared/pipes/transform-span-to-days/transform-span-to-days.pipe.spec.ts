import { TransformSpanToDaysPipe } from './transform-span-to-days.pipe';

describe('TransformSpanToDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformSpanToDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
